# Global header configuration
[[headers]]
for = "/*"
[headers.values]
Access-Control-Allow-Origin = "*"

# Specific header configuration for /web and paths under it
[[headers]]
for = "/godot/*"
[headers.values]
Cross-Origin-Embedder-Policy = "require-corp"
Cross-Origin-Opener-Policy = "same-origin"
